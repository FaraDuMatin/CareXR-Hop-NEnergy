using UnityEngine;

/// <summary>
/// GazeInteractable: DEPRECATED — pylon selection now uses ISDK_RayInteraction instead of gaze.
/// This script is kept as a placeholder. Remove the component from the camera in Unity.
/// </summary>
public class GazeInteractable : MonoBehaviour
{
    // Intentionally empty — replaced by PylonInteractable + RayInteraction.
    // You can safely remove this component from the Main Camera.
}
